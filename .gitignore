node_modules
target

# Local Node/Yarn binaries (not needed in repo)
node/

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

._*
.DS_Store

# Build output in src
src/main/resources/javascript/apps

# Credentials — never commit
.env